function openApp(){document.getElementById('landing').classList.add('hidden');document.getElementById('app').classList.remove('hidden')}
document.querySelectorAll('.nav').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.nav').forEach(b=>b.classList.remove('active'));document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));btn.classList.add('active');document.getElementById(btn.dataset.page).classList.add('active')}))
function checkAnswer(){let v=document.getElementById('answer').value.trim();let r=document.getElementById('result');if(v==='1000'||v==='1000₽'){r.style.color='green';r.textContent='Верно! 25% от 800 = 200, значит 800 + 200 = 1000 ₽.'}else{r.style.color='red';r.textContent='Почти. 25% от 800 = 200. Ответ: 1000 ₽.'}}
function sendChat(){let i=document.getElementById('chatInput');let log=document.getElementById('chatLog');let t=i.value.trim();if(!t)return;log.innerHTML+=`<div class="message user">${t}</div><div class="message bot-msg">Пока это демо-MVP. Настоящий AI можно подключить позже через API: он будет отвечать на вопросы, проверять решения и сохранять прогресс.</div>`;i.value=''}